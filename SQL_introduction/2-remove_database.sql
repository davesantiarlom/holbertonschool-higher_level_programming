-- This script deletes the database hbtn_0c_0 if it exists
-- Interestingly, the DELETE function is not the correct one to use here, as it only deletes content
-- within the database, not the database itself

DROP DATABASE IF EXISTS hbtn_0c_0
